Agent Environment: PageAI Chrome Extension

Цель этого документа — создать рабочую среду для ИИ-агентов, которые смогут:
	•	спроектировать
	•	реализовать
	•	протестировать
Chrome-расширение для умного поиска и выжимок в Confluence

Среда рассчитана на работу в VSCode / Cursor / Zed / NeoVim и ориентирована на agent-based разработку.

⸻

1. Общая цель проекта

Создать Chrome Extension, который:
	•	работает поверх Confluence (Cloud / Server)
	•	умеет искать по нескольким пространствам
	•	делает выжимки и синтез из нескольких страниц
	•	не отправляет данные в коммерческие облака
	•	использует локальные или user-provided модели
	•	минимизирует вычислительные и инфраструктурные затраты

⸻

2. Архитектурные принципы (обязательные)

Агенты ОБЯЗАНЫ соблюдать:
	1.	Local-first (данные не покидают машину пользователя)
	2.	Retrieval-first (никакого fine-tune под знания)
	3.	BYOM (Bring Your Own Model)
	4.	Zero backend by default
	5.	Deterministic pipeline (объяснимые шаги)

Нарушение принципов = архитектурная ошибка.

⸻

3. Структура проекта (обязательная)

page-ai/
├─ agent.md                 # главный файл инструкций для агентов
├─ README.md
├─ manifest.json
├─ src/
│  ├─ content/
│  │  └─ confluence.ts      # работа с DOM Confluence
│  ├─ background/
│  │  └─ index.ts           # orchestration, messaging
│  ├─ ui/
│  │  ├─ panel.html
│  │  ├─ panel.ts
│  │  └─ panel.css
│  ├─ search/
│  │  ├─ keyword.ts         # BM25 / keyword search
│  │  ├─ embedding.ts       # embeddings interface
│  │  └─ rerank.ts
│  ├─ llm/
│  │  ├─ client.ts          # OpenAI-compatible client
│  │  └─ prompts.ts
│  ├─ storage/
│  │  └─ indexdb.ts
│  └─ types/
├─ scripts/
│  └─ dev.mjs
└─ tests/


⸻

4. agent.md (инструкции для агентов)

Создай файл agent.md со следующим содержимым:

⸻

🎯 Роль агента

Ты — инженер-агент. Твоя задача — создать работающее Chrome-расширение, а не демо или концепт.

Цели
	1.	Реализовать extension, которое:
	•	читает страницы Confluence
	•	индексирует их локально
	•	ищет по нескольким пространствам
	•	делает выжимки и синтез
	2.	Минимизировать:
	•	количество LLM-вызовов
	•	использование памяти
	•	сложность инфраструктуры

Запрещено
	•	Использовать коммерческие API по умолчанию
	•	Отправлять данные на внешний сервер
	•	Fine-tune / LoRA под знания
	•	Смешивать поиск и генерацию

Разрешено
	•	Ollama / llama.cpp / LM Studio
	•	OpenAI-compatible локальные эндпоинты
	•	IndexedDB
	•	FAISS WASM (опционально)

⸻

Архитектура принятия решений

Каждый шаг должен быть:
	•	объясним
	•	детерминирован
	•	воспроизводим

Если возникает выбор:

Keyword search > Semantic rerank > LLM synthesis


⸻

Definition of Done

Фича считается готовой, если:
	•	работает в Chrome
	•	не требует сервера
	•	не нарушает NDA
	•	даёт ссылки на источники

⸻

Стиль кода
	•	TypeScript
	•	Functional core > imperative shell
	•	Явные типы
	•	Минимум зависимостей

⸻

Формат ответов агента
	1.	Что сделано
	2.	Почему так
	3.	Что осталось
	4.	Риски

⸻

5. Среда разработки

VSCode / Cursor

Рекомендуемые плагины:
	•	ESLint
	•	Prettier
	•	Chrome Extension Tools

Запуск
	1.	npm install
	2.	npm run dev
	3.	Chrome → Load unpacked → dist/

⸻

6. Минимальный MVP (агенты должны начать с этого)
	1.	Content script читает страницу Confluence
	2.	Сохраняет текст + metadata в IndexedDB
	3.	Keyword search по сохранённым страницам
	4.	Простая выжимка через локальную LLM
	5.	UI в side panel

⸻

7. Расширение функционала (поэтапно)
	•	Семантический rerank
	•	Кеширование summary
	•	Работа с несколькими space
	•	Сравнение страниц
	•	Timeline изменений

⸻

8. Критерии отказа

Если:
	•	требуется сервер
	•	требуется коммерческий API
	•	требуется дообучение модели

→ решение считается неправильным.

⸻

9. Ментальная модель

LLM — это:
	•	читатель
	•	синтезатор
	•	планировщик

НЕ:
	•	база знаний
	•	источник истины

⸻

10. Финальная проверка

Перед завершением:
	•	можно ли удалить страницу из индекса?
	•	можно ли показать источник?
	•	можно ли работать оффлайн?

Если нет — вернуться назад.

⸻

11. README.md (минимум)

README должен описывать:
	•	что делает расширение
	•	как подключить локальную модель
	•	как проверить NDA-безопасность

⸻

12. Тестирование

Минимум:
	•	manual tests
	•	fake Confluence page

⸻

Конец инструкции

Если ты читаешь это — ты уже инженер, а не экспериментатор.

⸻

5. Следующие шаги (для тебя)
	1.	Решить, нужен ли тебе второй агент — архитектор / ревьюер / тестировщик / продуктовик
	2.	Перейти к генерации starter-кода или развитию существующего кода
	3.	Разобрать конкретный API Confluence (permissions, REST, DOM)

Этот документ — точка старта, не потолок.

⸻

13. Multi-agent команда

В этом репозитории есть файл `AGENTS.md`, описывающий продуктовую команду агентов:
	•	Product Agent — формирует задачи и приоритеты
	•	Architect Agent — проектирует решения и хранит архитектурные принципы
	•	Implementation Agent — пишет код расширения
	•	Reviewer Agent — делает код-ревью
	•	QA Agent — тестирует расширение
	•	Docs Agent — поддерживает документацию
	•	Release Agent — готовит релизы

Каждый агент:
	•	читает этот `agent.md` как источник архитектурных ограничений
	•	соблюдает Local-first / Retrieval-first / BYOM / Zero backend
	•	отчитывается в формате:
		1. Что сделано
		2. Почему так
		3. Что осталось
		4. Риски