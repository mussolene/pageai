stages:
  - lint
  - build

variables:
  NODE_ENV: production

image: node:20-alpine

cache:
  paths:
    - node_modules/

before_script:
  - npm ci

lint:
  stage: lint
  script:
    - npm run lint || echo "Lint not configured yet"

build:
  stage: build
  script:
    - npm run build
